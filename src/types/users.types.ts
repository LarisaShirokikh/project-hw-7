/*export type UsersType = {
  accountData: {
    id: string;
    login: string;
    email: string;
    passwordHash: string;
    createdAt: Date;
    isConfirmed: boolean;
  };
  emailConfirmation: {
    email: string;
    confirmationCode: string;
    expirationDate: Date;
    isConfirmed: boolean;
  };
};

export type UsersEmailConfDataType = {
  email: string;
  confirmationCode: string;
  expirationDate: Date;
  isConfirmed: boolean;
};*/
